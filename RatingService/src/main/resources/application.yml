server:
  port: 8083

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/microservices
  application:
    name: RATING-SERVICE
  config:
    import: optional:configserver:http://localhost:8085 # configserver is a fixed keyword and optional means if my config server is down for any reason then my client MS should not get down.

#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    fetch-registry: true
#    register-with-eureka: true # By default, it's true you can remove it also.
#    service-url:
#      defaultZone: http://localhost:8761/eureka/ # It will be using this url to register to the server